# SPEC-0009 — Tasks
- [x] `lib/wikilinks.ts` + test
- [x] Nodo `wikilink` in `lib/markdown.ts` + test; `resolveLink` in `ui/Markdown`
- [x] Schema `notes` + migrazione
- [x] Query condivisa dei nomi dei personaggi in `db/queries`
- [x] Slice `notes`: query, azioni (rinomina che riscrive i collegamenti), componenti
- [x] Pagine: elenco con ricerca, nuova (con titolo precompilato), dettaglio con citazioni
- [x] AC1–AC9 provati nel browser
